interface:
  display_name: "Local Service Launcher"
  short_description: "用隐私隔离框架实时监控并验证 localhost 服务"
  default_prompt: "Use $local-service-launcher framework v1 for <paths>; materialize it at <destination>; keep all project data in that instance only; then verify lifecycle, monitoring, drift, and privacy."
